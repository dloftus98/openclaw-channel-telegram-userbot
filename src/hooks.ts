/**
 * Hook pack entrypoint.
 *
 * This plugin currently does not ship OpenClaw hooks, but OpenClaw's plugin
 * installer expects a declared `openclaw.hooks` entry for hook-pack
 * validation.
 */
export default []
